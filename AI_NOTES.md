# AI Notes

## 1. What was AI-generated vs. written by me

Everything in this repo — `src/app.py`, `tests/`, `README.md`, and this
file's first draft — was generated by Claude (Anthropic) in an interactive
chat session, in response to a plain-English description of the assignment
(add/view/filter/total/delete expenses, in-memory or JSON-file storage, any
stack).

Specifically, Claude:
- Chose Flask + a JSON file as the storage layer
- Wrote the five endpoints (`POST /expenses`, `GET /expenses`,
  `GET /expenses/<id>`, `DELETE /expenses/<id>`, `GET /expenses/total`)
  and their input validation
- Refactored the app into an application factory (`create_app()`) so the
  data file path is injectable, specifically to make it testable
- Wrote the pytest suite (23 tests) covering success paths, validation
  errors, filtering, deletion, and totals
- Wrote the README

> ⚠️ **Fill in before submitting:** if you changed, moved, renamed, or
> rewrote any part of this after generation — even small edits — list it
> here specifically. An evaluator reading this file wants to know where
> your own judgment entered the code, not just where Claude's did.

## 2. What I validated, tested, or changed, and why

Claude ran the following checks itself, in a sandboxed environment, before
handing the code over:
- Ran the full pytest suite in a **clean virtualenv** (fresh `pip install`
  from `requirements.txt`) — all 23 tests passed
- Started the server with `python src/app.py` and hit it with real `curl`
  requests to confirm it actually serves traffic, not just passes tests
  against the Flask test client
- Exercised edge cases by hand: negative amounts, missing fields, malformed
  dates, deleting a nonexistent id, filtering a category with no matches,
  totals on an empty dataset

> ⚠️ **Fill in before submitting:** the above is what Claude checked, not
> what you checked. An evaluator is specifically looking for your own
> review, not a restatement of the AI's. At minimum, before submitting you
> should:
> - Read `src/app.py` end to end and confirm you understand every line
> - Run the install/run/test commands yourself, on your own machine, from
>   a clean checkout — don't assume it works because it worked here
> - Try to break it: send it something the tests don't cover and see what
>   happens
> - Note anything you personally changed and *why* — a bug you found, a
>   design choice you disagreed with, a naming convention you preferred
> Replace this bullet list with your own account once you've actually done it.

## 3. AI suggestions I didn't use, and why

None were rejected in this session — the generated design (Flask,
application-factory pattern, JSON-file storage, UUID ids) was accepted
as-is against the assignment's stated constraints.

> ⚠️ **Fill in before submitting:** if, while reviewing, you disagreed with
> any choice Claude made — e.g. JSON file vs. SQLite, UUIDs vs. incrementing
> integers, the validation rules, the shape of the `/expenses/total`
> response — say so here, and say what you'd do instead and why. If you
> genuinely have no disagreements, it's more credible to say "I reviewed X,
> Y, Z and agreed with the approach because ..." than to leave this section
> looking untouched.
